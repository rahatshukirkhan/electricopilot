"""ElectriCopilot — auditable AI copilot for electrical design.

The numeric core (IEC 60364 cable & protective-device sizing) is deterministic
and fully traceable to norm clauses. LLMs (Gemini via OpenRouter) orchestrate,
explain and independently verify — they never originate the numbers.
"""

__version__ = "0.1.0"
